import Singleton from './Singleton';
/**
 * 埋点封装类
 * 包装灵犀SDK
 */
export default class AnalyticsUtil extends Singleton {
    private cid;
    private pvParam;
    private pvStartTime;
    private isLastUploadPd;
    private publicParam;
    private category;
    private logEnabled;
    constructor();
    /**
     * 开启日志
     * @param enabled
     */
    setLogEnabled(enabled: any): void;
    /**
     * 设置灵犀全局category
     * @param category
     */
    setCategory(category: string): void;
    /**
     * 设置公共参数Object
     * @param publicParam
     */
    setPublicParam(publicParam: any): void;
    /**
     * 上传pv事件
     * @param param
     * @param cid
     */
    uploadPageViewEvent(cid: string, param?: object): void;
    /**
     * 上传mv事件
     * @param bid
     * @param valLabObject
     * @param optionObject
     */
    uploadModuleViewEvent(bid: string, valLabObject?: object, optionObject?: object): void;
    /**
     * 上传mc事件
     * @param bid
     * @param valLabObject
     * @param optionObject
     */
    uploadModuleClickEvent(bid: string, valLabObject?: object, optionObject?: object): void;
    /**
     * 上传tag事件
     * @param bid
     * @param valLabObject
     */
    uploadTagEvent(bid: string, valLabObject?: object): void;
    /**
     * 适配PV
     * [框架内部调用]
     */
    adapterPageViewEvent(): void;
    adapterPageDisappearEvent(): void;
    private uploadPageDisappearEvent;
    private upload;
}
