import SceneBase from './SceneBase';
import Singleton from '../util/Singleton';
/**
 * 界面管理器
 */
export default class SceneManager extends Singleton {
    private scene;
    private preScene;
    private touchBlockLayer;
    /**
     * 显示界面
     * @param scene 界面对象
     */
    showScene(scene: SceneBase): void;
    /**
     * 返回当前界面
     */
    getScene(): SceneBase;
}
