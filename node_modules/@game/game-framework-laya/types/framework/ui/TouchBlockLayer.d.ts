/**
 * 触摸屏蔽器
 * 在它下面的节点都不会响应触屏
 */
export default class TouchBlockLayer extends Laya.Sprite {
    private _isGray;
    constructor(isGray?: boolean);
    onAwake(): void;
    showGray(isGray: any): void;
}
