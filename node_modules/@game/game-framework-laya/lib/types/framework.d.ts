
interface CallbackObject {
  caller: any;
  callback: (isSuccess: boolean, data: any, userData?: any) => void;
}

/**
 * userAgent拿到的对象数据
 */
interface UserAgentObject {
  appName: string;
  appVersion: string;
  platform: string;
  osVersion: string;
  titansxVersion: string;
}
declare class Pike {
  constructor(a: any, b: any);
  open();
  restart();
  onReady(func: Function);
  onClose(func: Function);
  reconnect();
  close();
  start();
  send(msg: object, successCallback: Function, failCallback: Function);
  sendMessage(msg: object);
  onConnect(func: Function);
  beforeOpen(func: Function);
  onLogin(func: Function);
  onDisconnect(func: Function);
  onStart(func: Function);
  onConnectFail(func: Function);
  onOpenFail(func: Function);
  onOpen(func: Function);
  onLoginFail(func: Function);
  beforeLogin(func: Function);
  onMessage(func: Function);
}

declare let GameGlobal: any;
